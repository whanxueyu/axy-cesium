默认使用项目当前配置的开发端口；本项目联调时优先使用已经启动的
`http://localhost:5173`。

## 本地协作与服务约定

- 不要擅自启动、停止或重启本地开发服务。
- 联调或浏览器验证时，优先使用用户已经手动启动的
  `http://localhost:5173`。
- 如果确实需要额外启动服务，只能使用 `5174` 或更高端口，不能占用、
  停止或重启现有服务，也不要使用 `5173`。
- 不要无必要地执行生产构建；只有在用户明确要求，或当前修改确实需要
  构建验证时才运行 `npm run build`。